;
; BIND data file for local loopback interface
;
$TTL    604800
@       IN      SOA     ns.ascorp.com. root.ascorp.com. (
                     <%= @serial %>     ; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL
;
		NS      ns
		NS	ns2
		NS	ns3
<% ascorp.each do |ascorp| -%>
<%= ascorp %>
<% end -%>
