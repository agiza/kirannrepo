        # Maximum request body size we will
        # accept for buffering
        SecRequestBodyLimit <%= @bodylimit %>

        # Store up to 128 KB in memory
        SecRequestBodyInMemoryLimit <%= @bodylimit %>

