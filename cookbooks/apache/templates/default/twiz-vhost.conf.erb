<VirtualHost *:80>
    ServerAdmin webmaster@altidev.com
    ServerName altisource.twiz.li
    DocumentRoot "/var/www/html/dev"

    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI}

    Include proxy.d/rtsslproxy.conf

</VirtualHost>
