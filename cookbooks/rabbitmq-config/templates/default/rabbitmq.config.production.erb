[{rabbit, [{cluster_nodes, [
<% curr_environ = node[:chef_environment] -%>
<% rabbitservers = search(:node, "role:rabbitserver" AND "chef_environment:QA") -%>
<% @rabbitservers.collect { |rabbitserver| "'rabbit@#{rabbitserver}'" }.join(",") -%>
]}]}].
