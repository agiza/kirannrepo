[{rabbit, [{cluster_nodes,
<% curr_environ = node[:chef_environment] -%>
<% rabbitservers = search(:node, "role:rabbitserver" AND "chef_environment:#{curr_environ}") -%>
['rabbit@<% rabbitservers.join (',') %>'
]}]}].
