#!/bin/bash
hosts="
<%- @hostentries.each do |entry| %>
<%= entry[:ipaddress] %> <%= entry[:fqdn] %> 
<%- end %>
"

for i in `echo $hosts`; do 
  check=`grep $i /etc/hosts`
  if [ "$check" == "" ]; then
    echo $i >> /etc/hosts
  fi
done


