#!/bin/bash

target=$1

if [ "$target" == "" ]; then
	target="realtrans-intdev"
fi

for i in `ls /home/ubuntu/test-files/`; do
	scp -r /home/ubuntu/test-files/$i root@$target:/opt/tomcat/BPO/input/
done
ssh root@$target "chown -R tomcat.tomcat /opt/tomcat/BPO"
echo "files copied"
