<Proxy *>
Order deny,allow
Allow from all
</Proxy>

ProxyRequests                                   Off
ProxyPreserveHost                               On
ProxyPass                                       /jira   http://<%= node[:jira_server] %>:<%= node[:jira_port] %>/jira
ProxyPassReverse                                /jira   http://<%= node[:jira_server] %>:<%= node[:jira_port] %>/jira


