#!/bin/bash
opt_check=`grep storage /etc/fstab`
disk_find=`sudo fdisk -l | grep 429.5 | cut -d " " -f2 | sed -e s/://g`
if [ "$opt_check" ==  "" ]; then
  sudo echo "$disk_find	/storage	auto	defaults,nobootwait,comment=cloudstorage	0	2" >> /etc/fstab
fi
sudo mount -a

