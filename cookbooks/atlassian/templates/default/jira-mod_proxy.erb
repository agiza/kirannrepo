<Proxy *>
Order deny,allow
Allow from all
</Proxy>

ProxyRequests                                   Off
ProxyPreserveHost                               On
<Location /jira>
ProxyPass   http://<%= node[:jira_server] %>:<%= node[:jira_port] %>/jira
</Location>

