<Proxy *>
Order deny,allow
Allow from all
</Proxy>

ProxyRequests                                   Off
ProxyPreserveHost                               On
<Location /confluence>
ProxyPass  http://<%= node[:confluence_server] %>:<%= node[:confluence_port] %>/confluence

</Location>
