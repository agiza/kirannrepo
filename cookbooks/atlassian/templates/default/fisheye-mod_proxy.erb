<Proxy *>
Order deny,allow
Allow from all
</Proxy>

ProxyRequests                Off
ProxyPreserveHost            On
ProxyPass                    /fisheye        http://<%= node[:fisheye_server] %>:<%= node[:fisheye_port] %>/fisheye
ProxyPassReverse             /fisheye        http://<%= node[:fisheye_server] %>:<%= node[:fisheye_port] %>/fisheye


