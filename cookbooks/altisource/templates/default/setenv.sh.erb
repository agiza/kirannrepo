#!/bin/sh

<% stringified_parameters = @parameters.join(' ') -%>

CATALINA_OPTS="<%= stringified_parameters %> -XX:-UseSplitVerifier"
JRE_HOME=/usr/java/default

# node specific commands
<%= node[:altisource][:altitomcat][:setenv_cmds] %>