#!/bin/sh

NAME=appdynamics

# Default for number of days to keep old log files in /opt/appdynamics/logs
LOGFILE_DAYS=15

if [ -d /opt/$NAME/logs ]; then
        find /opt/$NAME/logs/ -name \*.log -mtime +$LOGFILE_DAYS -print0 \
                | xargs --no-run-if-empty -0 rm --
fi

